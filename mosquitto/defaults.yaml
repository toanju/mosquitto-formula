# -*- coding: utf-8 -*-
# vim: ft=yaml
---
mosquitto:
  pkg:
    name: mosquitto
  rootgroup: root
  config: '/etc/mosquitto/mosquitto.conf'
  service:
    name: mosquitto
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
