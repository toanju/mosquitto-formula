# -*- coding: utf-8 -*-
# vim: ft=yaml
---
mosquitto:
  pkg:
    name: mosquitto
  rootgroup: root
  config: '/etc/mosquitto'
  service:
    name: mosquitto
  subcomponent:
    config: '/etc/mosquitto-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
